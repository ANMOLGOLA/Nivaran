---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/prisma/schema.prisma
  - backend/src/db.ts
  - backend/src/app.ts
  - backend/src/index.ts
  - backend/src/controllers/auth.controller.ts
  - backend/src/routes/auth.routes.ts
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/pages/Login.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Citizen can request OTP and see it via an on-screen toast notification"
    - "Citizen can verify OTP and log in successfully"
    - "Citizen session is stored and authenticated endpoints are accessible"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Database schemas for User and OtpSession"
    - path: "backend/src/controllers/auth.controller.ts"
      provides: "OTP generation and JWT verification logic"
    - path: "frontend/src/pages/Login.tsx"
      provides: "OTP request and verification UI card"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "/api/auth/request-otp"
      via: "Axios or fetch POST request"
      pattern: "request-otp"
    - from: "backend/src/controllers/auth.controller.ts"
      to: "prisma.otpSession"
      via: "Database read/write of OTP records"
      pattern: "prisma\\.otpSession"
---

<objective>
Setup the React frontend and Express backend boilerplate with Prisma ORM (PostgreSQL) and implement the OTP authentication flow with on-screen mock toasts.

Purpose: Set up the technical foundation and authentication for all subsequent pillars of Smart Bharat.
Output: Configured workspace, database connection, backend authentication APIs, and working Login screen.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Backend Boilerplate and Prisma Schema</name>
  <files>backend/package.json, backend/tsconfig.json, backend/prisma/schema.prisma, backend/src/db.ts, backend/src/app.ts, backend/src/index.ts</files>
  <read_first>.planning/phases/01-foundation-auth/01-RESEARCH.md</read_first>
  <action>
    Initialize the `backend` folder as an npm project.
    Install typescript, express, cors, helmet, dotenv, jsonwebtoken, @prisma/client, and their devDependencies.
    Configure tsconfig.json for modern Node.js and TypeScript.
    Initialize Prisma and define the `User` (with roles CITIZEN, OFFICER, ADMIN) and `OtpSession` models in prisma/schema.prisma.
    Create a global database client helper in `backend/src/db.ts` to prevent multiple Prisma client instances.
    Set up the basic Express app inside `backend/src/app.ts` with cors, helmet, and json parsing.
    Write `backend/src/index.ts` to bind and listen to a configured port (e.g., 5000).
  </action>
  <verify>Run npm run build to ensure compilation succeeds; verify prisma schema formatting is valid.</verify>
  <acceptance_criteria>
    - backend/package.json contains expected dependencies
    - backend/prisma/schema.prisma contains User and OtpSession models
    - backend/src/db.ts exports a single instance of PrismaClient
  </acceptance_criteria>
  <done>Express server compiles and database setup is ready</done>
</task>

<task type="auto">
  <name>Task 2: Build Express Authentication Endpoints</name>
  <files>backend/src/controllers/auth.controller.ts, backend/src/routes/auth.routes.ts, backend/src/app.ts</files>
  <read_first>backend/src/app.ts, backend/prisma/schema.prisma</read_first>
  <action>
    Write authentication controller endpoints:
      - `POST /api/auth/request-otp`: Accept phone number, generate 6-digit OTP, upsert OtpSession with 5 min expiration, and return mock OTP in JSON payload response.
      - `POST /api/auth/verify-otp`: Validate phone and OTP, find or create the User record in database (default role: CITIZEN), and sign a JWT containing userId, phone, and role.
    Register routes in `backend/src/routes/auth.routes.ts` and mount them in `backend/src/app.ts` under `/api/auth`.
  </action>
  <verify>Launch server and make HTTP requests to verify endpoints manually or via supertest.</verify>
  <acceptance_criteria>
    - POST /api/auth/request-otp returns mockOtp code
    - POST /api/auth/verify-otp returns success: true and jwt token
  </acceptance_criteria>
  <done>Authentication endpoints are functioning and verified</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Frontend with Vite, Tailwind CSS, and Shadcn</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css</files>
  <read_first>.planning/phases/01-foundation-auth/01-RESEARCH.md</read_first>
  <action>
    Initialize the `frontend` folder as a React + TypeScript Vite project.
    Install tailwindcss, postcss, autoprefixer, lucide-react, sonner, and react-router-dom.
    Set up tailwind.config.js with tricolor-inspired branding colors:
      - Saffron: #FF9933
      - White: #FFFFFF
      - Green: #138808
      - Navy: #0B1F3A
    Configure Shadcn UI prerequisites (components.json, tsconfig edits, and path mappings).
    Configure Vite proxy in `vite.config.ts` to redirect `/api` requests to backend (`http://localhost:5000`).
  </action>
  <verify>Run npm run build inside frontend to ensure everything compiles cleanly.</verify>
  <acceptance_criteria>
    - frontend/tailwind.config.js contains custom Navy, Saffron, and Green colors
    - vite.config.ts is configured with proxy to backend
  </acceptance_criteria>
  <done>Frontend project is compiled and proxy is established</done>
</task>

<task type="auto">
  <name>Task 4: Implement Login UI and OTP Mock Toast</name>
  <files>frontend/src/pages/Login.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx</read_first>
  <action>
    Build a highly polished, responsive Login page using Shadcn components (Card, Button, Input) styled with the custom tricolor theme.
    Add phone number input page, transitioning smoothly to OTP entry page.
    When user requests OTP:
      - Call `/api/auth/request-otp`
      - Capture the returned `mockOtp` from the response payload
      - Trigger Sonner toast notification displaying: `[Mock SMS Gateway] Your OTP is: XXX-XXX`
    When user submits OTP:
      - Call `/api/auth/verify-otp`
      - On success, store the JWT in local storage or state
      - Show success toast and redirect user to a simple authenticated mock landing page showing user details (role and phone).
    Update `frontend/src/App.tsx` to handle routing between Login and Dashboard.
  </action>
  <verify>Launch both backend and frontend dev servers, click through OTP login flow, verify toast displays mock code, and login succeeds.</verify>
  <acceptance_criteria>
    - Login screen is responsive and has tabor-inspired branding elements
    - Toast notification shows the mock OTP code immediately after request
    - Verifying OTP correctly authenticates and logs in the user
  </acceptance_criteria>
  <done>Login page fully complete and OTP flow working end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run backend tests to verify request-otp and verify-otp routes work.
- [ ] Frontend compiles without any errors (`npm run build`).
- [ ] Manual test shows mock OTP code toast displays and login succeeds.
</verification>

<success_criteria>
- All tasks completed
- Citizen and Officer roles can login using OTP verification mock toast
- Sensitive credentials/PII handled safely
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
